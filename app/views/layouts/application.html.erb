<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Star Wars</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <a class="navbar-brand">Star Wars Wiki</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item <%= 'active' if current_page?(root_path) %>">
            <%= link_to 'Characters', root_path, class: "nav-link" %>  <span class="sr-only">(current)</span>
          </li>
          <li class="nav-item <%= 'active' if current_page?(homeworlds_path) %>">
            <%= link_to 'Homeworlds', homeworlds_path, class: "nav-link" %>
          </li>
          <li class="nav-item <%= 'active' if current_page?(speciess_path) %>">
            <%= link_to 'Species', speciess_path, class: "nav-link" %>
          </li>
        </ul>
        <%= simple_form_for :search, url: search_index_path, class: "form-inline my-2 my-lg-0", method: "GET", html: { class: 'form-inline' } do |f| %>
          <%= f.input :name, input_html: {value: ""}, class: "form-control mr-sm-2", label: false %>
          <%= f.submit "Search", class: "btn btn-outline-success my-2 my-sm-0" %>
        <% end %>
      </div>
    </nav>
  <div class="row mr-0 ml-0 justify-content-center">
    <div class="col-8 mt-5">
    </div>
  </div>
  <%= yield %>
  </body>
</html>
